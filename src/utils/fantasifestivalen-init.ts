import { team, setTeam, artists, setArtists, rules, setRules, events, setEvents } from './fantasifestivalen-stores';

async function setup(): Promise<boolean> {
  Promise.all([
  ]);
  return true;
}
